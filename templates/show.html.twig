{% extends 'base.html.twig' %}

{% block main %}
    <h1>Title 😝 : {{ product.name }}</h1>
    <p>Description : {{ product.description }}</p>
    <p>Posté le : {{  product.date |date('Y-m-d') }}</p>

{% endblock %}
